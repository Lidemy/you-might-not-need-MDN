# 遍歷陣列 forEach

&nbsp;

## 基本使用

會把每一個元素都傳進來，當你想對陣列的每一項元素都做些什麼的時候很適合：

``` js
var arr = [1, 2, 3]
arr.forEach(element => {
  console.log(element)
})

// 1
// 2
// 3
```

forEach 偏向你要對元素「做事情」，如果你只是想把陣列的每個元素都轉成別的東西（例如說每一項都乘以 2，那 [map](/docs/array/map) 比較適合）
