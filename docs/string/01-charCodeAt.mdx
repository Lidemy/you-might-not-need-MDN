# 獲得字元碼 charCodeAt

&nbsp;

## 基本使用

當你想獲得一個字元的 UTF-16 code unit 的時候，可以這樣做：

``` js
console.log("A".charCodeAt(0)) // 65
```

想要從 code 還原成字元的時候，可以這樣做：

``` js
console.log(String.fromCharCode(65)) // A
```
